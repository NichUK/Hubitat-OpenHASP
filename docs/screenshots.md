# Screenshots

The screenshots in `docs/images/` show the expected Hubitat configuration pages.

Planned captures:

- `panel-device-preferences.png`: `OpenHASP Panel` MQTT, plate, idle, timer, and control JSON preferences.
- `manager-panel-section.png`: `OpenHASP Manager` panel selection.
- `manager-lighting-bindings.png`: Office and Bedroom child-to-device bindings.
- `manager-ufh-binding.png`: UFH timer child bound to the safe virtual switch.

When capturing from a live hub, mask MQTT passwords and any access tokens before committing.
