# Screenshots

The screenshots in `docs/images/` show the expected Hubitat configuration pages.

Current captures:

- `openhasp-manager-config.png`: `OpenHASP Manager` plate settings, MQTT connector settings, timer, and lighting-target rows.
- `openhasp-ufh-virtual-switch.png`: safe virtual UFH switch created by the manager app.

When capturing from a live hub, mask MQTT passwords, usernames, broker addresses, and any access tokens before committing.
